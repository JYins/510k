{
  "name": "510k-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"no lint configured\"",
    "serve": "npm --prefix ../shared run build && npm run build && firebase emulators:start --only functions,firestore,auth"
  },
  "dependencies": {
    "@510k/shared": "file:510k-shared-0.1.0.tgz",
    "@opentelemetry/api": "^1.9.0",
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.1.1",
    "graphql": "^16.9.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
